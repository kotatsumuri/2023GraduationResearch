{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "build",
      "command": "/opt/homebrew/bin/cmake",
      "args": ["--build", "build"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ],
  "version": "2.0.0"
}
